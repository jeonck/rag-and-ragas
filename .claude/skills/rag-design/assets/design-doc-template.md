# RAG 설계 문서 — <프로젝트명>

> 작성일: <YYYY-MM-DD> / 작성자: <이름> / 상태: 초안 | 검토중 | 확정
>
> 모든 결정에는 **근거 / 측정 지표 / 재검토 조건**이 붙는다. 셋 중 하나라도 못 쓰겠으면 아직 결정할 단계가 아니다 — 기본값으로 두고 측정 후 돌아온다.

## 1. 문제 정의

**무엇을 푸는가:** <한 문단. "누가 어떤 질문을 하면 무엇을 답한다">

**성공 기준:** <측정 가능하게. 예: 실제 질문 50문항 중 근거 있는 정답 85% 이상>

**범위 밖:** <명시적으로 안 하는 것. 이게 없으면 요구가 계속 늘어난다>

### 인터뷰 결과

| 항목 | 답 | 설계 영향 |
|---|---|---|
| 질문 유형 | | |
| 코퍼스 (수/용량/형식/언어/갱신) | | |
| 정답의 소재 (단일 / multi-hop) | | |
| 접근 제어 | | |
| 오답 비용 | | |
| 예산 (p95 지연 / 월 쿼리 / 비용 상한) | | |
| 신규 or 개선 | | |

### 가정

<확인하지 못한 것을 여기에 적는다. 나중에 틀렸을 때 어디를 다시 볼지 알 수 있다>

## 2. 골든셋

- 위치: `<경로>`
- 문항 수: <N> (dev <n> / holdout <n>)
- 구성: 실제 질문 <%>, 답 없음 <%>, multi-hop <%>, 파싱 취약부 <%>
- 출처: <실제 로그 / 담당자 작성 / LLM 합성 비율>
- 마지막 갱신: <날짜>

## 3. 파이프라인

```
<블록 다이어그램 또는 단계 나열>
```

### 3.1 파싱

```
결정:
근거:
측정:
재검토:
```

알려진 취약부: <표 / 스캔본 / 2단 조판 등>

### 3.2 청킹

```
결정: 크기 <N> 토큰 / 오버랩 <N>% / 경계 <문단|섹션|...>
근거:
대안:
측정: context_recall, context_precision
재검토:
```

### 3.3 메타데이터

| 필드 | 용도 | 필수 |
|---|---|---|
| doc_id | 인용 | ✓ |
| section | 컨텍스트 헤더 | ✓ |
| source_uri | 출처 링크 | ✓ |
| updated_at | 최신성 필터 | |
| acl | 접근 제어 | |

### 3.4 임베딩 / 인덱스

```
임베딩 모델: <ID + 버전>   ← 재인덱싱 시 필요하므로 정확히
차원 / 최대 입력 길이:
벡터 스토어:
BM25 인덱스:
근거:
재검토:
```

### 3.5 검색

```
방식: <하이브리드 RRF / 벡터 단독 / ...>
1차 top-k:
리랭커: <모델 / 없음>
최종 top-k:
필터: <acl 사전 필터 / 최신성 / doc_type>
쿼리 변환: <재작성 / 분해 / 없음>
근거:
측정: context_recall, context_precision, p95 지연
재검토:
```

### 3.6 생성

```
모델: <claude-opus-5 등>
effort / thinking:
max_tokens:
인용: <Citations API / 없음>
abstain 정책: <검색 점수 임계치 / 프롬프트 규칙>
프롬프트: <경로 또는 인라인>
근거:
측정: faithfulness, answer_relevancy
재검토:
```

## 4. 평가

| 지표 | 목표 | 현재 | 측정일 |
|---|---|---|---|
| context_recall | 0.85 | | |
| context_precision | 0.70 | | |
| faithfulness | 0.90 | | |
| answer_relevancy | 0.85 | | |
| p95 지연 | | | |
| 쿼리당 비용 | | | |

- 판정 모델: `<ID>` — **고정. 바꾸면 이전 점수와 비교 불가**
- 판정 임베딩: `<ID>`
- RAGAS 버전 핀: `ragas==<x.y.z>`
- 실행: `python scripts/ragas_eval.py --golden <경로> --pipeline <모듈:함수>`
- CI 게이트: <있음 / 없음>
- 의미 있는 변화로 볼 최소 폭: <0.05>

## 5. 비용·지연 추정

| 항목 | 쿼리당 | 월 (<N>쿼리 기준) |
|---|---|---|
| 임베딩 | | |
| 검색·리랭킹 | | |
| 생성 (입력/출력 토큰) | | |
| 합계 | | |

지연 분해: 검색 <ms> + 리랭킹 <ms> + 생성 첫 토큰 <ms> = p95 <ms>

## 6. 위험과 대응

| 위험 | 영향 | 대응 | 확인 방법 |
|---|---|---|---|
| 예: multi-hop 질문 비중이 예상보다 큼 | recall 하락 | 쿼리 분해 도입 | 골든셋 태그별 recall |
| 예: 표 파싱 실패 | 특정 문서군 전체 검색 불가 | 표 전용 추출 | 파싱 결과 육안 검수 |
| | | | |

## 7. 운영

- 인덱스 갱신: <주기 / 증분 여부 / 구 청크 삭제 절차>
- 로깅: <남기는 필드>
- 모니터링: <abstain 비율, 인용 0개 비율, p95, 비용>
- 접근 제어: <사전 필터 구현 위치>
- 롤아웃: <대상 / 단계>

## 8. 로드맵

| 단계 | 내용 | 완료 조건 |
|---|---|---|
| 1 | 골든셋 <N>문항 확보 | 정답 검증 완료 |
| 2 | 베이스라인 구축 + 첫 측정 | 4지표 기록 |
| 3 | <가장 낮은 지표> 개선 | 목표 달성 |
| 4 | 회귀 게이트 CI 연결 | 자동 실행 |
| 5 | 제한 롤아웃 | 실패 사례 <N>건 수집 |

## 변경 이력

| 날짜 | 변경 | 사유 | 지표 영향 |
|---|---|---|---|
| | | | |
